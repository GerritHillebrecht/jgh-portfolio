import { PixarMouseEffectDirective } from './pixar-mouse-effect.directive';

describe('PixarMouseEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new PixarMouseEffectDirective();
    expect(directive).toBeTruthy();
  });
});
